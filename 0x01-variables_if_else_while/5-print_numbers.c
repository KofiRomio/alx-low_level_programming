#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
*main - Entry point
*Return: always 0 (success)
*/
int main(void)
{
int b;
for (b = 0; b < 10; b++)
printf("%d", b);
printf("\n");
return (0);
}
